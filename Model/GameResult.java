package Model;


import Enum.Result;

// 승부 결과를 알고 있는 모델
public class GameResult {
	
	/*
	 * // 게임 결과 가지고 있는 속성 private Result gameResult = new Player().play();
	 * 
	 * // 승부 결과 반환 메서드 public Result getGameResult() { return gameResult; }
	 */

}
